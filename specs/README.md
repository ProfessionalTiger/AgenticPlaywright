# Specs

This directory is used for planning and documenting Playwright test scenarios.

It can contain scenario notes, feature summaries, or test strategy documents that support the test files in the `tests/` directory.
